import { DynamicCpnDirective } from './dynamic-cpn.directive';

describe('DynamicCpnDirective', () => {
  it('should create an instance', () => {
    const directive = new DynamicCpnDirective();
    expect(directive).toBeTruthy();
  });
});
